version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: echo build
  deploy:
    requires:
      - build
    steps:
      - run: echo deploy

workflows:
  version: 2
  commit:
    jobs:
      - build
      - deploy:
          filters:
            tags:
              only: /.*/
