version: 2
jobs:
  build:
    working_directory: ~/circle2test
    machine: true
    steps:
      - checkout
      - run: docker version
      - run: uname -a
      - run: docker run --rm --privileged hypriot/qemu-register
      - run: docker build -t armhttp .
