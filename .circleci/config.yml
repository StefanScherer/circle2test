version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: echo build
      - run: ls
      - run: |
        if [ -z "${CIRCLE_TAG}" ]; then
          echo "No version tag detected. Skip publishing."
          exit 0
        fi
        echo publish

workflows:
  version: 2
  commit:
    jobs:
      - build
