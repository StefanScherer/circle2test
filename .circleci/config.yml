version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: echo build
      - run: ls
  deploy:
    machine: true
    steps:
      - run: echo deploy
      - run: ls

workflows:
  version: 2
  commit:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            tags:
              only: /.*/
